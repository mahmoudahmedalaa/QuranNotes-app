# Handover Document - 2026-04-08

## 1. Session Summary
- **What was the user's goal?** To resolve pervasive caching issues, phantom folders, missing bundles, and white screens in React Native / Expo / Xcode builds.
- **What was accomplished?** Created powerful deep clean scripts (`npm run clean` and `npm run clean:deep`) that wipe out metro/watchman/haste caches, generated `ios` and `android` native folders, `DerivedData`, and `node_modules`. Updated project documentation (`AGENTS.md`, `.agent/LESSONS_LEARNED.md`) to reflect this new capability.
- **What's left to do?** The user has requested a handover for the next AI.

## 2. Files Changed
- `package.json`: Added `clean` and `clean:deep` npm scripts for aggressive project purging.
- `.agent/LESSONS_LEARNED.md`: Documented the Metro/Xcode caching lesson and detailed the standard procedure when caching issues arise.
- `AGENTS.md`: Added the `npm run clean:deep` command to the Quick Reference section.

## 3. Current State
- **What works?** The cleanup commands have been correctly injected into the environment. 
- **What's broken?** Nothing known at the moment; however, if build errors or ghost components reappear, the new scripts provide a reliable reset mechanism.
- **What's in progress?** This session focused solely on stabilizing the build and caching environment. The next feature is undefined.

## 4. Critical Context
- **Deep Caching Issues**: When the Xcode simulator produces white screens, missing jsbundles, or duplicate ghost folders (e.g., `ios/ios`), standard build tools fail because Metro/Watchman caching is too deeply entrenched.
- **Aggressive Reset**: The `npm run clean:deep` script deletes `ios`, `android`, and Xcode `DerivedData` completely, bypassing standard build debugging.

## 5. Recommended Next Steps
1. Run `npx expo prebuild --clean` to regenerate the iOS folder and Native code paths if entering a fresh development cycle.
2. Proceed to run `npm run ios` or equivalent Xcode steps to confirm the simulator handles the fresh environment correctly.
3. Review `AGENTS.md` and `.agent/LESSONS_LEARNED.md` if further caching edge-cases appear.

## 6. Commands to Run
```bash
# Verify everything is okay
npx tsc --noEmit
npm test -- --passWithNoTests

# Re-prep the project structure if needed:
# npx expo prebuild --clean
```
